name: Firebase

on:
  push:
    branches: [main]
    paths:
      - functions/**
      - firestore.rules
      - storage.rules
      - packages/types/**

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ vars.TURBO_TEAM }}

jobs:
  rules-test:
    name: Firestore & Storage rules
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with: { node-version: 20, cache: npm }
      - run: npm install --legacy-peer-deps
      - name: Install Firebase emulator
        run: npx firebase-tools setup:emulators:firestore --project demo-test
      - name: Run rules unit tests
        run: |
          npx firebase emulators:exec \
            --only firestore,storage \
            --project demo-test \
            'npm run test:rules'

  functions-typecheck:
    name: Functions typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with: { node-version: 20, cache: npm }
      - run: npm install --legacy-peer-deps && cd functions && npm install --legacy-peer-deps
      - run: cd functions && npx tsc --noEmit

  deploy:
    name: Deploy
    needs: [rules-test, functions-typecheck]
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with: { node-version: 20, cache: npm }
      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.FIREBASE_SA_EMAIL }}
      - run: npm install --legacy-peer-deps && cd functions && npm install --legacy-peer-deps && npm run build
      - name: Deploy functions + rules
        run: |
          npx firebase-tools deploy \
            --only functions,firestore:rules,storage:rules \
            --project ${{ vars.FIREBASE_PROJECT_ID }} \
            --force
