import { CoverReconClient } from './PageClient';

export const metadata = {
  title: 'Cover Charge | Finance | Venue Dashboard',
  description: 'Canonical Cover Charge wallet reconciliation for your venue.',
};

export default function VenueCoverChargePage() {
  return <CoverReconClient />;
}
