import VenueFinanceReportsClient from "./PageClient";

export const metadata = {
    title: "Reports | Finance | Venue Dashboard",
    description: "Download financial reports, payout summaries, and revenue exports.",
};

export default function VenueFinanceReportsPage() {
    return <VenueFinanceReportsClient />;
}
